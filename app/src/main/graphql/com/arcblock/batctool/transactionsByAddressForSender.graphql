query transactionsByAddressForSender($sender: String, $paging: PageInput) {
  transactionsByAddress(sender: $sender, paging: $paging) {
    data {
      hash
      blockHeight
      numberOutputs
      numberInputs
    }
    page {
      cursor
      next
      total
    }
  }
}